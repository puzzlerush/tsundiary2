import React from 'react';
import { Button } from '@material-ui/core';

const LoginPage = () => (
    <div className="login">
        <Button size="large" variant="outlined">Log in with Google</Button>
    </div>
);

export default LoginPage;